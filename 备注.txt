最小允许宽度 375xp 
小于 1024px lg 时，nav bar 移动到屏幕底部。点击搜索按钮跳转到搜索页面，而不是
弹出搜索栏推荐
在 768px md 和 1024px lg 之间时，nav bar 悬浮在底部，不占满屏幕宽度
小于 768px md 时，nav bar 在屏幕底部，占满屏幕宽度


搜索栏推荐：
当屏幕宽度大于 1700px 3xl 时，卡片4列布局；
当屏幕宽度小于 1700px 3xl 时，卡片3列布局；

搜索栏推荐卡片：
长宽均随容器缩放
屏幕宽度小于 1024px lg 时，字体大小为14px text-sm；